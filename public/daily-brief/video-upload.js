(()=>{const form=document.querySelector('[data-video-upload]');if(!form)return;const status=document.querySelector('[data-upload-status]');const progress=document.querySelector('[data-upload-progress]');const say=(message,isError=false)=>{status.textContent=message;status.dataset.error=isError?'true':'false'};const json=async(response)=>{const data=await response.json().catch(()=>({}));if(!response.ok)throw Error(data.error||'Request failed.');return data};form.addEventListener('submit',async event=>{event.preventDefault();const fields=new FormData(form);const file=fields.get('video');const password=String(fields.get('password')||'');if(!(file instanceof File)||file.type!=='video/mp4'){say('Choose the MP4 saved from TikTok.',true);return}if(file.size>20000000){say('This MP4 is larger than 20 MB. Export a smaller copy before uploading.',true);return}const button=form.querySelector('button');button.disabled=true;progress.hidden=false;progress.value=5;try{say('Preparing today’s Daily Brief entry…');const start=await json(await fetch('/api/daily-video/start-upload',{method:'POST',headers:{authorization:'Bearer '+password,'content-type':'application/json'},body:JSON.stringify({title:fields.get('title'),description:fields.get('description'),privacyStatus:fields.get('privacyStatus'),tiktokUrl:fields.get('tiktokUrl'),fileSize:file.size})}));progress.value=15;say('Uploading to the Daily Brief… Keep this page open.');const saved=await new Promise((resolve,reject)=>{const xhr=new XMLHttpRequest();xhr.open('PUT',start.uploadUrl);xhr.setRequestHeader('Authorization','Bearer '+password);xhr.setRequestHeader('Content-Type','video/mp4');xhr.upload.onprogress=e=>{if(e.lengthComputable)progress.value=15+Math.round((e.loaded/e.total)*80)};xhr.onerror=()=>reject(Error('The video upload was interrupted.'));xhr.onload=()=>{let data={};try{data=JSON.parse(xhr.responseText)}catch{}if(xhr.status>=200&&xhr.status<300)resolve(data);else reject(Error(data.error||'The Daily Brief rejected the video.'))};xhr.send(file)});progress.value=100;sessionStorage.setItem('sapiver-video-password',password);say('Saved. The video is now in today’s Daily Brief and queued for the next YouTube publication run.');form.reset()}catch(error){say(error.message||'Upload failed.',true)}finally{button.disabled=false}});const remembered=sessionStorage.getItem('sapiver-video-password');if(remembered)form.elements.password.value=remembered})();